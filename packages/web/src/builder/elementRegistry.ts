/**
 * Central palette + layout helpers for the builder.
 * Builder components are wired in ElementWrapper (avoids circular imports with nested layouts).
 */
export { isLayoutType, PALETTE_ENTRIES } from "./elementMeta";
